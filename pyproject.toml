[project]
name = "hitman"
version = "0.1.0"
description = "Local API testing client"
requires-python = ">=3.9"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "jinja2>=3.1",
    "httpx>=0.28",
    "python-multipart>=0.0.20",
]

[project.scripts]
hitman = "hitman.cli:main"

[dependency-groups]
dev = ["pytest>=8.3", "ruff>=0.8"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["hitman"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
# Keep the floor honest: this app must run on the system Python that ships
# with macOS and most Linux distributions, where installing a newer CPython
# may not be permitted.
target-version = "py39"
